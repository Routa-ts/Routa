# Routa Dex

Dex task state belongs here once the v0 backlog is initialized.

Rules:

- Commit curated v0 task state.
- Keep design decisions in `docs/`.
- Do not store scratch notes or private agent reasoning.
