---
title: Routa
description: Schema-first, OpenAPI-aware REST framework for new TypeScript APIs.
---

import { Aside, CardGrid, LinkCard } from "@astrojs/starlight/components";
import PackageManagers from "@components/PackageManagers.astro";

Routa is a schema-first, OpenAPI-aware REST framework for new TypeScript APIs. Choose your
entry point based on whether you want a generated starter, need to add Routa to an
existing TypeScript API, or already have an OpenAPI contract.

## Options

### Start From a Generated Project

Use the scaffolder when you want the fastest path to a working Routa application:

<PackageManagers type="create" />

You receive a status route, tests, configuration, and committed `.routa/` metadata. The
tradeoff is adopting Routa's generated project shape before you have chosen each file.

### Add Routa to an Existing API

Follow [Installation](/docs/getting-started/installation/) when you already have a
TypeScript codebase and want to migrate one HTTP boundary at a time. You keep control of
the surrounding application, but you must add the scripts, configuration entry point,
and generated metadata yourself.

### Start From OpenAPI

Use [Scaffold from OpenAPI](/docs/guides/openapi-scaffold/) when a reviewed OpenAPI
document is the starting contract. Routa generates the first source representation and a
baseline; after that import, route files become the source of truth and regeneration must
be previewed.

## What Routa Does

Routes are filesystem-backed. Folders map to URL segments, and each `route.ts` file owns
every method for that path.

```ts title="src/routes/users/route.ts"
import { createRoute, createRouteRoot } from "@routa-ts/core";
import { CreateUserSchema, UserSchema } from "./schemas.js";
import * as users from "../../services/users.js";

const route = createRouteRoot("/users");

export default route({
	post: createRoute({
		input: {
			body: CreateUserSchema,
		},
		responses: {
			success: {
				status: 201,
				schema: UserSchema,
			},
		},
		run: async ({ input }) => {
			return { type: "success", data: await users.createUser(input.body) };
		},
	}),
});
```

`input` describes what the request must contain. Routa parses `params`, `query`,
`headers`, `cookies`, and `body` through those Zod schemas before the handler runs, so
`input.body` is already validated and typed.

`responses` is a map of named outcomes to a status and a schema. The handler returns one
of those names with its data. Returning an undeclared name is a type error, and returning
data that does not match the schema is caught at runtime before it reaches the client.

Everything else is derived. The CLI reads these files to generate route metadata, produce
OpenAPI, and check that the contract has not drifted from its committed baseline.

## Consequences

- One route contract drives TypeScript types, runtime validation, generated metadata, and
  OpenAPI, so contract changes appear in several reviewable outputs.
- Filesystem paths and `createRouteRoot(path)` must agree; moving a route is an API change.
- Business logic does not belong in `run`. Routa owns routing, validation, middleware
  context, typed responses, and OpenAPI, while services, domain models, and persistence
  stay application-owned.
- `.routa/` is committed source-derived state, not a disposable build artifact.

## When This Changes

<Aside type="caution" title="v0 status">
	Routa is at a v0 implementation baseline: Hono runtime, Zod schemas, JSON request and
	response bodies. See [v0 Scope & Status](/docs/start/v0-scope/) for the full line
	between what ships and what is deferred.
</Aside>

## Next Steps

<CardGrid>
	<LinkCard
		title="Quickstart"
		href="/docs/start/quickstart/"
		description="Create a project and run the generated status route."
	/>
	<LinkCard
		title="Why Routa"
		href="/docs/start/why-routa/"
		description="The problem this design solves and the tradeoffs it accepts."
	/>
	<LinkCard
		title="Installation"
		href="/docs/getting-started/installation/"
		description="Add Routa to a new or existing TypeScript API."
	/>
	<LinkCard
		title="Route contracts"
		href="/docs/concepts/route-contracts/"
		description="Read the model behind every Routa HTTP boundary."
	/>
</CardGrid>
