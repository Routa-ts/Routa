---
"@routa-ts/core": minor
"@routa-ts/cli": minor
"create-routa-ts": minor
---

Add configurable runtime response validation, validate responses by default in development, preserve opt-in production validation, project middleware context through declared schemas, log safe structured schema issue paths, and reject thrown raw `Response` bypasses.
